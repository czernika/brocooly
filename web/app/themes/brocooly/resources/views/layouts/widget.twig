{{ args.before_title|raw }}
	{{ instance.title|apply_filters('widget_title') }}
{{ args.after_title|raw }}

{% block widget %}
	<div>
		{{ __('No widget instance was passed', 'brocooly') }}
	</div>
{% endblock %}
